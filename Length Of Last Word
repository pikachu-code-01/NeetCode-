class Solution {
    public int lengthOfLastWord(String s) {
        s=s.trim();
        int count =0;
        for(int co = s.length() -1;co>=0;co--)
        {
            if(s.charAt(co) == ' '){ break;}
            else count++;
        }
        return count;
    }
}
